<?php
/**
 * @file
 * A description of what your module does.
 */

/**
 * Implements hook_menu().
 *
 *   Adds a setting page for this module, where admins can choose
 *   a base currency and one or more switchable currencies.
 */
function fixer_io_menu() {

  $items['admin/config/regional/fixer_io'] = array(
    'title' => t('fixer.io Currency Switcher'),
    'description' => t('Choose a base currency.'),
    'weight' => 100,
    'page callback' => 'drupal_get_form',
    'page arguments' => array('fixer_io_settings'),
    'access arguments' => array('administer site configuration'),
    'file' => 'fixer_io.admin.inc'
  );

  return $items;
}

/**
 * Implements hook_cron().
 *
 *   Updates exchange rates on cron run.
 */
function fixer_io_cron() {

  _fixer_io_reload();
}

/**
 * Implements hook_field_formatter_info().
 *
 *   Provides the new field formatter for decimal number fields.
 */
function fixer_io_field_formatter_info() {

  $info = array(
    'fixer_io' => array(
      'label' => t('fixer.io currency switcher'),
      'description' => t('Displays value with currency switcher.'),
      'field types' => array('number_decimal'),
    ),
  );

  return $info;
}

/**
 * Implements hook_field_formatter_view().
 *   Outputs the currency switcher form.
 */
function fixer_io_field_formatter_view($entity_type, $entity, $field, $instance, $langcode, $items, $display) {

  $element = array(); // Initialize the var
  $settings = $display['settings']; // get the settings

  switch ($display['type']) {
    case 'fixer_io':

      foreach ($items as $delta => $item) {

        $value = $item['value'];
        $switcher_form = drupal_get_form('fixer_io_switcher', $value);

        $element[$delta] = array(
          '#markup' => render($switcher_form),
        );
      }
      break;
  }

  return $element;
}

/**
 * Currency switcher form handler.
 *
 *   Returns the actual switcher form.
 */
function fixer_io_switcher($form, &$form_state, $value) {

  $symbol_position = variable_get('fixer_io_symbol_position', 'before');
  $default_iso_code = variable_get('fixer_io_base', 'EUR');
  $fixer_io_options = variable_get('fixer_io_options', array(
    'EUR',
    'USD',
    'GBP'
  ));

  $options = array();
  foreach ($fixer_io_options as $key => $iso) {
    if ($iso != '0') {
      $options[$key] = $iso;
    }
  }

  // Ensures unique HTML IDs ...
  $id = drupal_html_id('fixer-io-container');
  // ... which will get renewed on every AJAX callback.
  $form['fixer_io_container'] = array(
    '#prefix' => '<div id="' . $id . '">', // THIS ID.
    '#type' => 'container',
    '#suffix' => '</div>'
  );

  if ($symbol_position == 'before') {
    $form['fixer_io_container']['fixer_io_symbol_before'] = array(
      '#prefix' => '<span class="form-item fixer-io symbol">',
      '#markup' => (!empty($form_state['values']['fixer_io_switcher']) ? ($form_state['values']['fixer_io_switcher'] != $default_iso_code ? _fixer_io_symbol($form_state['values']['fixer_io_switcher']) : _fixer_io_symbol($default_iso_code)) : _fixer_io_symbol($default_iso_code)),
      '#suffix' => '</span>',
    );
  }

  $form['fixer_io_container']['fixer_io_value'] = array(
    '#prefix' => '<span class="form-item fixer-io value">',
    '#markup' => (!empty($form_state['values']['fixer_io_switcher']) ? ($form_state['values']['fixer_io_switcher'] != $default_iso_code ? _fixer_io_calculate($value, $form_state['values']['fixer_io_switcher']) : $value) : $value),
    '#suffix' => '</span>',
  );

  if ($symbol_position == 'after') {
    $form['fixer_io_container']['fixer_io_symbol_after'] = array(
      '#prefix' => '<span class="form-item fixer-io symbol">',
      '#markup' => (!empty($form_state['values']['fixer_io_switcher']) ? ($form_state['values']['fixer_io_switcher'] != $default_iso_code ? _fixer_io_symbol($form_state['values']['fixer_io_switcher']) : _fixer_io_symbol($default_iso_code)) : _fixer_io_symbol($default_iso_code)),
      '#suffix' => '</span>',
    );
  }

  $form['fixer_io_container']['fixer_io_switcher'] = array(
    '#prefix' => '<div class="form-item fixer-io-inline fixer-io select">',
    '#type' => 'select',
    '#options' => drupal_map_assoc($options),
    '#default_value' => $default_iso_code,
    '#ajax' => array(
      'callback' => 'fixer_io_switcher_ajax_callback',
      'wrapper' => $id, // AND THIS ID.
    ),
    '#suffix' => '</div>',
  );

  return $form;
}

/**
 * Callback for both ajax-enabled buttons.
 *
 *   Returns the whole fixer.io container after the switcher got changed.
 */
function fixer_io_switcher_ajax_callback($form, $form_state) {

  return $form['fixer_io_container'];
}

/**
 * Helper function
 *   which calculates one currency value into another currency.
 *
 * @param $value number.
 * @param $iso_code three-letters currency ISO code.
 * @return calculated number.
 */
function _fixer_io_calculate($value, $iso_code) {

  $obj = variable_get('fixer_io_rates');

  return number_format($obj->rates->$iso_code * $value, 2, '.', '');
}

/**
 * Helper function
 *   which pulls current exchange rates from fixer.io and
 *   stores them in the database.
 */
function _fixer_io_reload() {

  // Get current rates from fixer.io
  if ($json = file_get_contents('http://api.fixer.io/latest?base=' . variable_get('fixer_io_base', 'EUR'))) {
    $obj = json_decode($json);
    variable_set('fixer_io_rates', $obj);
  }
}

/**
 * Helper function
 *   which returns currency symbol from given ISO code.
 *   Array taken from @see https://gist.github.com/Gibbs/3920259
 *
 * @param $iso_code three-letters currency ISO code.
 * @return HTML currency symbol.
 */
function _fixer_io_symbol($iso_code) {

  $currency_symbols = array(
    'AED' => '&#1583;.&#1573;', // ?
    'AFN' => '&#65;&#102;',
    'ALL' => '&#76;&#101;&#107;',
    'AMD' => '',
    'ANG' => '&#402;',
    'AOA' => '&#75;&#122;', // ?
    'ARS' => '&#36;',
    'AUD' => '&#36;',
    'AWG' => '&#402;',
    'AZN' => '&#1084;&#1072;&#1085;',
    'BAM' => '&#75;&#77;',
    'BBD' => '&#36;',
    'BDT' => '&#2547;', // ?
    'BGN' => '&#1083;&#1074;',
    'BHD' => '.&#1583;.&#1576;', // ?
    'BIF' => '&#70;&#66;&#117;', // ?
    'BMD' => '&#36;',
    'BND' => '&#36;',
    'BOB' => '&#36;&#98;',
    'BRL' => '&#82;&#36;',
    'BSD' => '&#36;',
    'BTN' => '&#78;&#117;&#46;', // ?
    'BWP' => '&#80;',
    'BYR' => '&#112;&#46;',
    'BZD' => '&#66;&#90;&#36;',
    'CAD' => '&#36;',
    'CDF' => '&#70;&#67;',
    'CHF' => '&#67;&#72;&#70;',
    'CLF' => '', // ?
    'CLP' => '&#36;',
    'CNY' => '&#165;',
    'COP' => '&#36;',
    'CRC' => '&#8353;',
    'CUP' => '&#8396;',
    'CVE' => '&#36;', // ?
    'CZK' => '&#75;&#269;',
    'DJF' => '&#70;&#100;&#106;', // ?
    'DKK' => '&#107;&#114;',
    'DOP' => '&#82;&#68;&#36;',
    'DZD' => '&#1583;&#1580;', // ?
    'EGP' => '&#163;',
    'ETB' => '&#66;&#114;',
    'EUR' => '&#8364;',
    'FJD' => '&#36;',
    'FKP' => '&#163;',
    'GBP' => '&#163;',
    'GEL' => '&#4314;', // ?
    'GHS' => '&#162;',
    'GIP' => '&#163;',
    'GMD' => '&#68;', // ?
    'GNF' => '&#70;&#71;', // ?
    'GTQ' => '&#81;',
    'GYD' => '&#36;',
    'HKD' => '&#36;',
    'HNL' => '&#76;',
    'HRK' => '&#107;&#110;',
    'HTG' => '&#71;', // ?
    'HUF' => '&#70;&#116;',
    'IDR' => '&#82;&#112;',
    'ILS' => '&#8362;',
    'INR' => '&#8377;',
    'IQD' => '&#1593;.&#1583;', // ?
    'IRR' => '&#65020;',
    'ISK' => '&#107;&#114;',
    'JEP' => '&#163;',
    'JMD' => '&#74;&#36;',
    'JOD' => '&#74;&#68;', // ?
    'JPY' => '&#165;',
    'KES' => '&#75;&#83;&#104;', // ?
    'KGS' => '&#1083;&#1074;',
    'KHR' => '&#6107;',
    'KMF' => '&#67;&#70;', // ?
    'KPW' => '&#8361;',
    'KRW' => '&#8361;',
    'KWD' => '&#1583;.&#1603;', // ?
    'KYD' => '&#36;',
    'KZT' => '&#1083;&#1074;',
    'LAK' => '&#8365;',
    'LBP' => '&#163;',
    'LKR' => '&#8360;',
    'LRD' => '&#36;',
    'LSL' => '&#76;', // ?
    'LTL' => '&#76;&#116;',
    'LVL' => '&#76;&#115;',
    'LYD' => '&#1604;.&#1583;', // ?
    'MAD' => '&#1583;.&#1605;.', //?
    'MDL' => '&#76;',
    'MGA' => '&#65;&#114;', // ?
    'MKD' => '&#1076;&#1077;&#1085;',
    'MMK' => '&#75;',
    'MNT' => '&#8366;',
    'MOP' => '&#77;&#79;&#80;&#36;', // ?
    'MRO' => '&#85;&#77;', // ?
    'MUR' => '&#8360;', // ?
    'MVR' => '.&#1923;', // ?
    'MWK' => '&#77;&#75;',
    'MXN' => '&#36;',
    'MYR' => '&#82;&#77;',
    'MZN' => '&#77;&#84;',
    'NAD' => '&#36;',
    'NGN' => '&#8358;',
    'NIO' => '&#67;&#36;',
    'NOK' => '&#107;&#114;',
    'NPR' => '&#8360;',
    'NZD' => '&#36;',
    'OMR' => '&#65020;',
    'PAB' => '&#66;&#47;&#46;',
    'PEN' => '&#83;&#47;&#46;',
    'PGK' => '&#75;', // ?
    'PHP' => '&#8369;',
    'PKR' => '&#8360;',
    'PLN' => '&#122;&#322;',
    'PYG' => '&#71;&#115;',
    'QAR' => '&#65020;',
    'RON' => '&#108;&#101;&#105;',
    'RSD' => '&#1044;&#1080;&#1085;&#46;',
    'RUB' => '&#1088;&#1091;&#1073;',
    'RWF' => '&#1585;.&#1587;',
    'SAR' => '&#65020;',
    'SBD' => '&#36;',
    'SCR' => '&#8360;',
    'SDG' => '&#163;', // ?
    'SEK' => '&#107;&#114;',
    'SGD' => '&#36;',
    'SHP' => '&#163;',
    'SLL' => '&#76;&#101;', // ?
    'SOS' => '&#83;',
    'SRD' => '&#36;',
    'STD' => '&#68;&#98;', // ?
    'SVC' => '&#36;',
    'SYP' => '&#163;',
    'SZL' => '&#76;', // ?
    'THB' => '&#3647;',
    'TJS' => '&#84;&#74;&#83;', // ? TJS (guess)
    'TMT' => '&#109;',
    'TND' => '&#1583;.&#1578;',
    'TOP' => '&#84;&#36;',
    'TRY' => '&#8356;', // New Turkey Lira (old symbol used)
    'TTD' => '&#36;',
    'TWD' => '&#78;&#84;&#36;',
    'TZS' => '',
    'UAH' => '&#8372;',
    'UGX' => '&#85;&#83;&#104;',
    'USD' => '&#36;',
    'UYU' => '&#36;&#85;',
    'UZS' => '&#1083;&#1074;',
    'VEF' => '&#66;&#115;',
    'VND' => '&#8363;',
    'VUV' => '&#86;&#84;',
    'WST' => '&#87;&#83;&#36;',
    'XAF' => '&#70;&#67;&#70;&#65;',
    'XCD' => '&#36;',
    'XDR' => '',
    'XOF' => '',
    'XPF' => '&#70;',
    'YER' => '&#65020;',
    'ZAR' => '&#82;',
    'ZMK' => '&#90;&#75;', // ?
    'ZWL' => '&#90;&#36;',
  );

  return $currency_symbols[$iso_code];
}
